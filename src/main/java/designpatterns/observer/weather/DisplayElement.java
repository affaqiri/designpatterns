package designpatterns.observer.weather;

/**
 * We should always strive to program to an interface (java interface or
 * abstract class).
 */
public interface DisplayElement {
	
	public void display();

}